"""CV ranking system package.

Phases 1-4 implemented (simple local mode):

- Ingestion/OCR -> Markdown
- Extraction -> schema-bound CV JSON
- Local embedding index + ranking (no DB)
- LLM judge + FastAPI API layer
"""
